## Updating State Based on Current State

Use callback function for more safety

```jsx
function handlePrevious() {
  if (step > 1)
    setStep((currentStep) => {
      return currentStep - 1;
    });
}
```

[Next: More Thoughts About State + State Guidelines](./11.md)
